# There is nothing to edit over here; all global styling is handled in "/src/config/styles";

if you want to view global variable output, go look at "./variables.output.css" or "./variables.output.scss";
